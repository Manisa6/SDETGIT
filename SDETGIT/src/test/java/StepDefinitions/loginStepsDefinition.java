package StepDefinitions;

import java.time.Duration;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;
import org.testng.Assert;

import io.cucumber.java.en.And;
import io.cucumber.java.en.Given;
import io.cucumber.java.en.Then;
import io.cucumber.java.en.When;

public class loginStepsDefinition {
	/*
	 * static WebDriver driver;
	 * 
	 * @Given("User is on login page") public void user_is_on_login_page() { driver
	 * = new ChromeDriver();
	 * driver.manage().timeouts().implicitlyWait(Duration.ofSeconds(30));
	 * driver.get("https://www.saucedemo.com/v1/");
	 * driver.manage().window().maximize();
	 * 
	 * }
	 * 
	 * @When("User enters valid username and password") public void
	 * user_enters_valid_username_and_password() {
	 * driver.findElement(By.id("user-name")).sendKeys("standard_user");
	 * driver.findElement(By.id("password")).sendKeys("secret_sauce"); }
	 * 
	 * @When("User enters valid {string} , {string}") public void
	 * user_enters_valid_and(String username, String password) {
	 * driver.findElement(By.id("user-name")).sendKeys(username);
	 * driver.findElement(By.id("password")).sendKeys(password);
	 * 
	 * }
	 * 
	 * 
	 * @And("Click on login Button") public void click_on_login_button() {
	 * driver.findElement(By.id("login-button")).click();
	 * 
	 * }
	 * 
	 * @Then("User should navigated to home page") public void
	 * user_should_navigated_to_home_page() { Assert.assertTrue(
	 * driver.findElements(By.className("app_logo")).size()>
	 * 0,"User should navigated to home page"); }
	 * 
	 * @And("Close browser") public void close_browser() { driver.close(); }
	 * 
	 */
}
